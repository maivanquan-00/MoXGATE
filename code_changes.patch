diff --git a/train.py b/train.py
index e5a28b4..3658717 100644
--- a/train.py
+++ b/train.py
@@ -242,11 +242,16 @@ def train(args):
     subtype_names = ["CIN", "GS", "MSI", "HM-SNV", "EBV"]
     present_labels = sorted(np.unique(np.concatenate([test_targets, test_preds])))
     present_names  = [subtype_names[i] for i in present_labels]
-    print(f"\nTest Accuracy : {test_metrics['accuracy']:.4f}")
-    print(f"Test Precision: {test_metrics['precision']:.4f}")
-    print(f"Test Recall   : {test_metrics['recall']:.4f}")
-    print(f"Test Weighted F1: {test_metrics['weighted_f1']:.4f}")
-    print(f"Test Macro F1   : {test_metrics['macro_f1']:.4f}")
+    print(f"\n{'='*75}")
+    print("  TEST SET METRICS (FINAL)")
+    print(f"{'='*75}")
+    print(f"{'Metric':<20} | {'Macro':<20} | {'Weighted':<20}")
+    print(f"{'-'*75}")
+    print(f"{'Accuracy':<20} | {test_metrics['accuracy']:.4f}         | {test_metrics['accuracy']:.4f}")
+    print(f"{'F1-Score':<20} | {test_metrics['macro_f1']:.4f}         | {test_metrics['weighted_f1']:.4f}")
+    print(f"{'Precision':<20} | {test_metrics.get('macro_precision', 0.0):.4f}         | {test_metrics['precision']:.4f}")
+    print(f"{'Recall':<20} | {test_metrics.get('macro_recall', 0.0):.4f}         | {test_metrics['recall']:.4f}")
+    print(f"{'='*75}")
     print(f"\nModality weights (final): {model.get_modality_weights()}")
     print(f"\nClassification Report:\n{classification_report(test_targets, test_preds, labels=present_labels, target_names=present_names, zero_division=0)}")
 
diff --git a/train_kfold.py b/train_kfold.py
index 527bd90..679983f 100644
--- a/train_kfold.py
+++ b/train_kfold.py
@@ -227,12 +227,15 @@ def main():
         avg_rec = np.mean([x["macro_recall"] for x in seed_metrics])
         std_rec = np.std([x["macro_recall"] for x in seed_metrics])
         
-        print(f"\n>>> SEED {seed} AVERAGE ┬▒ STD <<<")
-        print(f"  F1-Macro         : {avg_macro:.4f} ┬▒ {std_macro:.4f}")
-        print(f"  F1-Weighted      : {avg_weight:.4f} ┬▒ {std_weight:.4f}")
-        print(f"  Accuracy         : {avg_acc:.4f} ┬▒ {std_acc:.4f}")
-        print(f"  Precision-Macro  : {avg_pre:.4f} ┬▒ {std_pre:.4f}")
-        print(f"  Recall-Macro     : {avg_rec:.4f} ┬▒ {std_rec:.4f}")
+        print(f"\n>>> SEED {seed} FINAL METRICS (Mean ┬▒ Std) - SOFTMAX <<<")
+        print(f"{'='*75}")
+        print(f"{'Metric':<20} | {'Macro':<25} | {'Weighted':<25}")
+        print(f"{'-'*75}")
+        print(f"{'Accuracy':<20} | {avg_acc:.4f} ┬▒ {std_acc:.4f}       | {avg_acc:.4f} ┬▒ {std_acc:.4f}")
+        print(f"{'F1-Score':<20} | {avg_macro:.4f} ┬▒ {std_macro:.4f}       | {avg_weight:.4f} ┬▒ {std_weight:.4f}")
+        print(f"{'Precision':<20} | {avg_pre:.4f} ┬▒ {std_pre:.4f}       | {np.mean([x['weighted_precision'] for x in seed_metrics]):.4f} ┬▒ {np.std([x['weighted_precision'] for x in seed_metrics]):.4f}")
+        print(f"{'Recall':<20} | {avg_rec:.4f} ┬▒ {std_rec:.4f}       | {np.mean([x['weighted_recall'] for x in seed_metrics]):.4f} ┬▒ {np.std([x['weighted_recall'] for x in seed_metrics]):.4f}")
+        print(f"{'='*75}")
         
         all_results.append({
             "seed": seed,
@@ -260,18 +263,25 @@ def main():
     ov_acc = np.mean([x["accuracy"] for x in all_folds_metrics])
     sd_acc = np.std([x["accuracy"] for x in all_folds_metrics])
     
-    ov_pre = np.mean([x["macro_precision"] for x in all_folds_metrics])
-    sd_pre = np.std([x["macro_precision"] for x in all_folds_metrics])
+    ov_pre_m = np.mean([x["macro_precision"] for x in all_folds_metrics])
+    sd_pre_m = np.std([x["macro_precision"] for x in all_folds_metrics])
+    ov_pre_w = np.mean([x["weighted_precision"] for x in all_folds_metrics])
+    sd_pre_w = np.std([x["weighted_precision"] for x in all_folds_metrics])
     
-    ov_rec = np.mean([x["macro_recall"] for x in all_folds_metrics])
-    sd_rec = np.std([x["macro_recall"] for x in all_folds_metrics])
+    ov_rec_m = np.mean([x["macro_recall"] for x in all_folds_metrics])
+    sd_rec_m = np.std([x["macro_recall"] for x in all_folds_metrics])
+    ov_rec_w = np.mean([x["weighted_recall"] for x in all_folds_metrics])
+    sd_rec_w = np.std([x["weighted_recall"] for x in all_folds_metrics])
 
-    print("OVERALL FINAL METRICS (Mean ┬▒ Std):")
-    print(f"  F1-Macro         : {ov_mac:.4f} ┬▒ {sd_mac:.4f}")
-    print(f"  F1-Weighted      : {ov_wei:.4f} ┬▒ {sd_wei:.4f}")
-    print(f"  Accuracy         : {ov_acc:.4f} ┬▒ {sd_acc:.4f}")
-    print(f"  Precision-Macro  : {ov_pre:.4f} ┬▒ {sd_pre:.4f}")
-    print(f"  Recall-Macro     : {ov_rec:.4f} ┬▒ {sd_rec:.4f}")
+    print("\nOVERALL FINAL METRICS (Mean ┬▒ Std) - SOFTMAX:")
+    print(f"{'='*75}")
+    print(f"{'Metric':<20} | {'Macro':<25} | {'Weighted':<25}")
+    print(f"{'-'*75}")
+    print(f"{'Accuracy':<20} | {ov_acc:.4f} ┬▒ {sd_acc:.4f}       | {ov_acc:.4f} ┬▒ {sd_acc:.4f}")
+    print(f"{'F1-Score':<20} | {ov_mac:.4f} ┬▒ {sd_mac:.4f}       | {ov_wei:.4f} ┬▒ {sd_wei:.4f}")
+    print(f"{'Precision':<20} | {ov_pre_m:.4f} ┬▒ {sd_pre_m:.4f}       | {ov_pre_w:.4f} ┬▒ {sd_pre_w:.4f}")
+    print(f"{'Recall':<20} | {ov_rec_m:.4f} ┬▒ {sd_rec_m:.4f}       | {ov_rec_w:.4f} ┬▒ {sd_rec_w:.4f}")
+    print(f"{'='*75}")
     
     with open(args.save_path, "w") as f:
         json.dump(all_results, f, indent=2)
diff --git a/train_kfold_sparse.py b/train_kfold_sparse.py
index 77fe40b..6352089 100644
--- a/train_kfold_sparse.py
+++ b/train_kfold_sparse.py
@@ -228,12 +228,15 @@ def main():
         avg_rec = np.mean([x["macro_recall"] for x in seed_metrics])
         std_rec = np.std([x["macro_recall"] for x in seed_metrics])
         
-        print(f"\n>>> SEED {seed} AVERAGE ┬▒ STD (SPARSEMAX) <<<")
-        print(f"  F1-Macro         : {avg_macro:.4f} ┬▒ {std_macro:.4f}")
-        print(f"  F1-Weighted      : {avg_weight:.4f} ┬▒ {std_weight:.4f}")
-        print(f"  Accuracy         : {avg_acc:.4f} ┬▒ {std_acc:.4f}")
-        print(f"  Precision-Macro  : {avg_pre:.4f} ┬▒ {std_pre:.4f}")
-        print(f"  Recall-Macro     : {avg_rec:.4f} ┬▒ {std_rec:.4f}")
+        print(f"\n>>> SEED {seed} FINAL METRICS (Mean ┬▒ Std) - SPARSEMAX <<<")
+        print(f"{'='*75}")
+        print(f"{'Metric':<20} | {'Macro':<25} | {'Weighted':<25}")
+        print(f"{'-'*75}")
+        print(f"{'Accuracy':<20} | {avg_acc:.4f} ┬▒ {std_acc:.4f}       | {avg_acc:.4f} ┬▒ {std_acc:.4f}")
+        print(f"{'F1-Score':<20} | {avg_macro:.4f} ┬▒ {std_macro:.4f}       | {avg_weight:.4f} ┬▒ {std_weight:.4f}")
+        print(f"{'Precision':<20} | {avg_pre:.4f} ┬▒ {std_pre:.4f}       | {np.mean([x['weighted_precision'] for x in seed_metrics]):.4f} ┬▒ {np.std([x['weighted_precision'] for x in seed_metrics]):.4f}")
+        print(f"{'Recall':<20} | {avg_rec:.4f} ┬▒ {std_rec:.4f}       | {np.mean([x['weighted_recall'] for x in seed_metrics]):.4f} ┬▒ {np.std([x['weighted_recall'] for x in seed_metrics]):.4f}")
+        print(f"{'='*75}")
         
         all_results.append({
             "seed": seed,
@@ -261,18 +264,25 @@ def main():
     ov_acc = np.mean([x["accuracy"] for x in all_folds_metrics])
     sd_acc = np.std([x["accuracy"] for x in all_folds_metrics])
     
-    ov_pre = np.mean([x["macro_precision"] for x in all_folds_metrics])
-    sd_pre = np.std([x["macro_precision"] for x in all_folds_metrics])
+    ov_pre_m = np.mean([x["macro_precision"] for x in all_folds_metrics])
+    sd_pre_m = np.std([x["macro_precision"] for x in all_folds_metrics])
+    ov_pre_w = np.mean([x["weighted_precision"] for x in all_folds_metrics])
+    sd_pre_w = np.std([x["weighted_precision"] for x in all_folds_metrics])
     
-    ov_rec = np.mean([x["macro_recall"] for x in all_folds_metrics])
-    sd_rec = np.std([x["macro_recall"] for x in all_folds_metrics])
+    ov_rec_m = np.mean([x["macro_recall"] for x in all_folds_metrics])
+    sd_rec_m = np.std([x["macro_recall"] for x in all_folds_metrics])
+    ov_rec_w = np.mean([x["weighted_recall"] for x in all_folds_metrics])
+    sd_rec_w = np.std([x["weighted_recall"] for x in all_folds_metrics])
 
-    print("OVERALL FINAL METRICS (Mean ┬▒ Std) ΓÇö SPARSEMAX:")
-    print(f"  F1-Macro         : {ov_mac:.4f} ┬▒ {sd_mac:.4f}")
-    print(f"  F1-Weighted      : {ov_wei:.4f} ┬▒ {sd_wei:.4f}")
-    print(f"  Accuracy         : {ov_acc:.4f} ┬▒ {sd_acc:.4f}")
-    print(f"  Precision-Macro  : {ov_pre:.4f} ┬▒ {sd_pre:.4f}")
-    print(f"  Recall-Macro     : {ov_rec:.4f} ┬▒ {sd_rec:.4f}")
+    print("\nOVERALL FINAL METRICS (Mean ┬▒ Std) - SPARSEMAX:")
+    print(f"{'='*75}")
+    print(f"{'Metric':<20} | {'Macro':<25} | {'Weighted':<25}")
+    print(f"{'-'*75}")
+    print(f"{'Accuracy':<20} | {ov_acc:.4f} ┬▒ {sd_acc:.4f}       | {ov_acc:.4f} ┬▒ {sd_acc:.4f}")
+    print(f"{'F1-Score':<20} | {ov_mac:.4f} ┬▒ {sd_mac:.4f}       | {ov_wei:.4f} ┬▒ {sd_wei:.4f}")
+    print(f"{'Precision':<20} | {ov_pre_m:.4f} ┬▒ {sd_pre_m:.4f}       | {ov_pre_w:.4f} ┬▒ {sd_pre_w:.4f}")
+    print(f"{'Recall':<20} | {ov_rec_m:.4f} ┬▒ {sd_rec_m:.4f}       | {ov_rec_w:.4f} ┬▒ {sd_rec_w:.4f}")
+    print(f"{'='*75}")
     
     with open(args.save_path, "w") as f:
         json.dump(all_results, f, indent=2)
diff --git a/train_new.py b/train_new.py
index 6c703d5..75506b3 100644
--- a/train_new.py
+++ b/train_new.py
@@ -116,7 +116,17 @@ def train(args):
     present_labels = sorted(np.unique(np.concatenate([test_targets, test_preds])))
     present_names = [GI_SUBTYPE_NAMES[i] for i in present_labels]
 
-    print(f"\n[Softmax New Split Result] Accuracy: {test_metrics['accuracy']:.4f} | F1: {test_metrics['weighted_f1']:.4f}")
+    print(f"\n{'='*75}")
+    print("  TEST SET METRICS (FINAL) - Softmax New Split")
+    print(f"{'='*75}")
+    print(f"{'Metric':<20} | {'Macro':<20} | {'Weighted':<20}")
+    print(f"{'-'*75}")
+    print(f"{'Accuracy':<20} | {test_metrics['accuracy']:.4f}         | {test_metrics['accuracy']:.4f}")
+    print(f"{'F1-Score':<20} | {test_metrics['macro_f1']:.4f}         | {test_metrics['weighted_f1']:.4f}")
+    print(f"{'Precision':<20} | {test_metrics.get('macro_precision', 0.0):.4f}         | {test_metrics['precision']:.4f}")
+    print(f"{'Recall':<20} | {test_metrics.get('macro_recall', 0.0):.4f}         | {test_metrics['recall']:.4f}")
+    print(f"{'='*75}")
+    print("\nDetailed Classification Report:")
     print(classification_report(test_targets, test_preds, target_names=present_names, zero_division=0))
     
     cm_path = os.path.join(args.save_dir, "confusion_matrix_gi_new.png")
diff --git a/train_sparse.py b/train_sparse.py
index fc9a105..91d69e1 100644
--- a/train_sparse.py
+++ b/train_sparse.py
@@ -166,8 +166,18 @@ def train(args):
     present_labels = sorted(np.unique(np.concatenate([test_targets, test_preds])))
     present_names = [GI_SUBTYPE_NAMES[i] for i in present_labels]
 
-    print(f"\n[Sparsemax Result - GI] Accuracy: {test_metrics['accuracy']:.4f} | F1: {test_metrics['weighted_f1']:.4f}")
-    print(f"Classification Report:\n{classification_report(test_targets, test_preds, labels=present_labels, target_names=present_names, zero_division=0)}")
+    print(f"\n{'='*75}")
+    print("  TEST SET METRICS (FINAL) - Sparsemax")
+    print(f"{'='*75}")
+    print(f"{'Metric':<20} | {'Macro':<20} | {'Weighted':<20}")
+    print(f"{'-'*75}")
+    print(f"{'Accuracy':<20} | {test_metrics['accuracy']:.4f}         | {test_metrics['accuracy']:.4f}")
+    print(f"{'F1-Score':<20} | {test_metrics['macro_f1']:.4f}         | {test_metrics['weighted_f1']:.4f}")
+    print(f"{'Precision':<20} | {test_metrics.get('macro_precision', 0.0):.4f}         | {test_metrics['precision']:.4f}")
+    print(f"{'Recall':<20} | {test_metrics.get('macro_recall', 0.0):.4f}         | {test_metrics['recall']:.4f}")
+    print(f"{'='*75}")
+    print(f"\nDetailed Classification Report:")
+    print(f"{classification_report(test_targets, test_preds, labels=present_labels, target_names=present_names, zero_division=0)}")
     
     cm_path = os.path.join(args.save_dir, "confusion_matrix_sparse_gi.png")
     plot_confusion_matrix(test_targets, test_preds, labels=present_names, save_path=cm_path)
diff --git a/train_sparse_new.py b/train_sparse_new.py
index 6b00ff0..c0f981c 100644
--- a/train_sparse_new.py
+++ b/train_sparse_new.py
@@ -115,7 +115,17 @@ def train(args):
     present_labels = sorted(np.unique(np.concatenate([test_targets, test_preds])))
     present_names = [GI_SUBTYPE_NAMES[i] for i in present_labels]
 
-    print(f"\n[Sparsemax New Split Result] Accuracy: {test_metrics['accuracy']:.4f} | F1: {test_metrics['weighted_f1']:.4f}")
+    print(f"\n{'='*75}")
+    print("  TEST SET METRICS (FINAL) - Sparsemax New Split")
+    print(f"{'='*75}")
+    print(f"{'Metric':<20} | {'Macro':<20} | {'Weighted':<20}")
+    print(f"{'-'*75}")
+    print(f"{'Accuracy':<20} | {test_metrics['accuracy']:.4f}         | {test_metrics['accuracy']:.4f}")
+    print(f"{'F1-Score':<20} | {test_metrics['macro_f1']:.4f}         | {test_metrics['weighted_f1']:.4f}")
+    print(f"{'Precision':<20} | {test_metrics.get('macro_precision', 0.0):.4f}         | {test_metrics['precision']:.4f}")
+    print(f"{'Recall':<20} | {test_metrics.get('macro_recall', 0.0):.4f}         | {test_metrics['recall']:.4f}")
+    print(f"{'='*75}")
+    print("\nDetailed Classification Report:")
     print(classification_report(test_targets, test_preds, target_names=present_names, zero_division=0))
     
     cm_path = os.path.join(args.save_dir, "confusion_matrix_sparse_gi_new.png")
